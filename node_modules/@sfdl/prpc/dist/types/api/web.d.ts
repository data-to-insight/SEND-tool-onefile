import { APIImplementation, APICallback, APIConfig, ResolverPromise } from '../types';
declare class WebApi implements APIImplementation {
    config: APIConfig | null;
    init(apiConfig: APIConfig, onResponse: APICallback): Promise<void>;
    handler(method: string, value: any): Promise<ResolverPromise>;
}
declare const api: WebApi;
export default api;
